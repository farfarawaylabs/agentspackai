interface:
  display_name: "Dev Flow"
  short_description: "Research, plan, implement, review, and verify"
  default_prompt: "Use $ap-dev-flow to take this goal from research through an approved plan to a reviewed, verified branch."
policy:
  allow_implicit_invocation: false
