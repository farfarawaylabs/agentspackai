schema_version = 1
name = "ap-design-critic"
description = "Independent read-only visual design critic for rendered interfaces. Use after a design has been rendered to judge product fit, visual identity, composition, typography, hierarchy, distinctiveness, restraint, and craft from screenshots or other explicit visual artifacts without inspecting implementation source."

[execution]
filesystem = "read-only"
reasoning_effort = "high"
